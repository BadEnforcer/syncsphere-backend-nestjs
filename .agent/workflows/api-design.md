---
description: Simple rules when working with API designing and building
---

1. When creating a new API or modifying paths, make sure paths do not conflict with each other. Such as, a dynamic userId path variable is caching a static path.